/* Template for contact/config.js (this file IS committed; config.js is NOT).
   Copy to config.js and set your real W3Forms access key for local testing,
   or have your deploy pipeline generate config.js from a secret. */
window.W3FORMS_ACCESS_KEY = "w3f_your_access_key_here";
